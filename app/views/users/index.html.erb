<h1>登録ユーザー一覧</h1>

<hr>

<table class="table table-bordered table-hover">
  <thead>
     <tr class="info"><th>id</th><th>名前</th></tr>
  </thead>
  <tbody>
    <% @users.each do |u| %>
    <tr>
    <td><%= u.id %></td>
    <td><%= u.name %></td>
    </tr>
    <% end %>
  </tbody>
</table>
